package com.wayapaychat.wayapay.presentation.screens.auth.forget_password

/*
@AndroidEntryPoint
class ForgetPasswordActivity : SwapActivity<ForgotPasswordActivityBinding>(R.layout.forgot_password_activity) {

    @Inject
    lateinit var forgotPasswordEmailFragment: ForgotPasswordEmailFragment

    @Inject
    lateinit var resetPasswordOtpFragment: ResetPasswordOtpFragment

    @Inject
    lateinit var changePasswordFragment: ChangePasswordFragment

    @Inject
    lateinit var forgetPasswordSuccessFragment: ForgetPasswordSuccessFragment
    override fun init() {
        initView()
    }

    private fun initView() {
        loadFragment()
    }

    @Suppress("UNCHECKED_CAST")
    private fun loadFragment() {
        // stack all fragment
        fragments.add(NavigationData(forgotPasswordEmailFragment as BaseNavigationFragment<ViewDataBinding>))
        fragments.add(NavigationData(resetPasswordOtpFragment as BaseNavigationFragment<ViewDataBinding>))
        fragments.add(NavigationData(changePasswordFragment as BaseNavigationFragment<ViewDataBinding>))
        fragments.add(NavigationData(forgetPasswordSuccessFragment as BaseNavigationFragment<ViewDataBinding>))

        for (fragment in fragments) {
            fragment.fragment.setSignUpContract(this)
        }
    }
}
*/
